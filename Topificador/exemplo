top
  string nome = 'Joao'
  int i = 0.0
  int a, b, c
  boolean vdd = true, invdd = false

  func olaMundo = [string nome] {
    comprimento = 'ola'
    return cumprimento + nome
  }

  olaMundo(nome)

  // comentario

  while i < 5.0 {
    print('Numero' + i)
    i = i + 1.0
  }

  for int i = 0.0; i < 5.0; i = i + 1.0 {
    print('Numero' + i)
    i = i + 1.0
  }

  a = 2.0
  b = 2.0
  c = a + b - 3.0 * (3.0 - 2.0)

  if a > b {
    print('ok')
  }
end