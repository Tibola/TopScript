
  let nome = 'Joao'
  let i = 0.0
  let a, b, c
  let vdd = true, invdd = false

let olaMundo = ( nome) => {
    cumprimento = 'ola'
    return cumprimento + nome
  }

  console.log(olaMundo(nome))

  // comentario

while ( i < 5.0) { 
    console.log('Numero' + i)
    i = i + 1.0
  }

for (let i = 0.0;  i < 5.0;  i = i + 1.0 ) { 
    console.log('Numero' + i)
    i = i + 1
  }

  a = 2.0
  b = x = a + b - 3.0 * (3.0 - 2.0)

if ( a > b ) {
    console.log('ok')
  }

